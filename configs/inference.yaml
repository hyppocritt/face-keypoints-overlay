model:
  path: ./weights/facepoints_resnet_nme_0_0469.pth
  filename: facepoints_resnet_nme_0_0469.pth
  # only change if trained on another input_size previously
  input_size: 224
  model_type: resnet

data:
  path: ./data/
  metadata_path: ./data/gt.csv
  chunk_size: 256

detector:
  device: cuda

inference:
  save: false
  save_dir: ./output/
  metric: null
  gt_path: null
  vis: null
  save_vis: false


detect:
  batch_size: 16
  use_amp: false
  pin_memory: true