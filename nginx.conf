server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /overlay {
        proxy_pass http://api:8000/overlay;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}